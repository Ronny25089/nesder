security:
  user:
    name: nesder
    password: nesder
    
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/nesder
    username: nesder
    password: nesder
    driverClassName: org.postgresql.Driver

server:
  port: 80
