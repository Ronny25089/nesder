security:
  user:
    name: nesder
    password: nesder
    
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/nesder
    username: postgres
    password: root
    driverClassName: org.postgresql.Driver

server:
  port: 80
